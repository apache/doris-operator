apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "doris-operator.fullname" . }}-test-connection"
  labels:
    {{- include "doris-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "doris-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
